#ifndef TYPES
#define TYPES

#include <cstdlib>
#include <cstdio>
#include <list>
#include <cassert>
#include <cstring>

struct CNode;
struct Chunk;
struct Tag;
struct BNode;
struct Element;
struct Bitset;
struct Cardinalities;
struct TotalOrder;
struct WBBTree;

#endif
